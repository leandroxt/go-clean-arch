# Go project architecture proposal

Feel free to propose another approaches.


## Run 

```
    GIPHY_API_KEY=your_key make run
```

Or create an `.env` file in the root and include in the `Makefile` (first row):

`include .env`